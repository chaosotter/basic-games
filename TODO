Fix indent on header of column (modern version).
Fix indent on header of cup (modern version).
Fix "left" -> "felt" in doctorz (modern version).
Dodgem is broken in Dosbox (both versions hang).
Convoy is having update issue on IA.
